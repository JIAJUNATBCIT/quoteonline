{"ast":null,"code":"import { PermissionUtils } from '../utils/permission.utils';\nimport * as i0 from \"@angular/core\";\nexport let PermissionService = /*#__PURE__*/(() => {\n  class PermissionService {\n    // 检查用户是否可以查看询价单\n    canViewQuote(quote, user) {\n      return PermissionUtils.canViewQuote(quote, user);\n    }\n    // 检查用户是否可以编辑询价单\n    canEditQuote(quote, user) {\n      return PermissionUtils.canEditQuote(quote, user);\n    }\n    // 检查用户是否可以删除询价单\n    canDeleteQuote(quote, user) {\n      return PermissionUtils.canDeleteQuote(quote, user);\n    }\n    // 检查用户是否可以拒绝询价单\n    canRejectQuote(quote, user) {\n      return PermissionUtils.canRejectQuote(quote, user);\n    }\n    // 检查用户是否可以删除文件\n    canDeleteFile(quote, fileType, user) {\n      return PermissionUtils.canDeleteFile(fileType, quote, user);\n    }\n    // 检查用户是否可以分配询价单\n    canAssignQuote(user) {\n      return user?.role === 'admin';\n    }\n    // 检查用户是否可以上传供应商文件\n    canUploadSupplierFile(quote, user) {\n      return user?.role === 'supplier' && (quote.status === 'in_progress' || quote.status === 'rejected') && quote.supplier && (quote.supplier._id === user.id || quote.supplier === user.id);\n    }\n    // 检查用户是否可以上传最终报价文件\n    canUploadFinalQuote(quote, user) {\n      return (user?.role === 'quoter' || user?.role === 'admin') && ['pending', 'supplier_quoted', 'in_progress'].includes(quote.status);\n    }\n    // 检查用户是否可以下载文件\n    canDownloadFile(quote, fileType, user) {\n      if (!user) return false;\n      switch (user.role) {\n        case 'customer':\n          if (fileType === 'customer') {\n            return quote.customer._id === user.id || quote.customer === user.id;\n          }\n          if (fileType === 'quoter') {\n            return quote.status === 'quoted' && (quote.customer._id === user.id || quote.customer === user.id);\n          }\n          return false;\n        // 客户永远不能下载供应商文件\n        case 'supplier':\n          // 供应商可以下载：1. 待处理的询价单的客户文件 2. 自己已报价的询价单的客户文件 3. 自己上传的供应商文件\n          return fileType === 'customer' && (quote.status === 'pending' || quote.supplier && (quote.supplier._id === user.id || quote.supplier === user.id)) || fileType === 'supplier' && quote.supplier && (quote.supplier._id === user.id || quote.supplier === user.id);\n        case 'quoter':\n        case 'admin':\n          return true;\n        // 报价员和管理员可以下载所有文件\n        default:\n          return false;\n      }\n    }\n    static {\n      this.ɵfac = function PermissionService_Factory(t) {\n        return new (t || PermissionService)();\n      };\n    }\n    static {\n      this.ɵprov = /*@__PURE__*/i0.ɵɵdefineInjectable({\n        token: PermissionService,\n        factory: PermissionService.ɵfac,\n        providedIn: 'root'\n      });\n    }\n  }\n  return PermissionService;\n})();","map":null,"metadata":{},"sourceType":"module","externalDependencies":[]}